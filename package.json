{
  "name": "@rosance/backend",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.0.1"
  },
  "scripts": {
    "start": "lerna run --parallel start",
    "stop": "lerna run --parallel stop"
  }
}
